# Files paths
import os

d_initPath = os.sep.join(os.path.realpath(__file__).split(os.sep)[:-1]) + os.sep + 'd_init.ini'
initPath = os.sep.join(os.path.realpath(__file__).split(os.sep)[:-1]) + os.sep + 'init.ini'
qc_robotPath = os.sep.join(os.path.realpath(__file__).split(os.sep)[:-1]) + os.sep + 'qc_robot.ini'
